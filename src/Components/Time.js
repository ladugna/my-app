const Time = () => {
    const currentTime = new Date().toLocaleTimeString();
    console.log(currentTime); // You can replace `console.log` with your desired implementation
    
    return currentTime;
  };
  
  export default Time;